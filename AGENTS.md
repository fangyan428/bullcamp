# BullCamp 项目约定

以用户当前指令为准。实施范围见 docs/实施计划.md。

- 核心课程 46 个任务全部可离线学习。真实 API/开源 Agent 运行只是补充，不能阻塞主线。
- 新版独立部署到 GitHub Pages，发布包不包含原版内容与入口；保留 MIT 许可与作者归属。仓库中的原版历史源文件不参与发布，新版本用独立存档键。
- 教学数据和 Agent 示例必须明确标注，不能把编写的示例说成真实运行。
- 财务计算与界面分开，使用确定的函数；开放判断不能仅凭关键词宣称正确。
- 中文用户文案；先有足够讲解与示范，再检查新知识。任务要有资料、操作、反馈和迁移题。
- 不读取或提交 API 密钥，不进行真实证券交易。外部服务实践与课程完成分别记录。
- 验证课程结构、模型和学习流程。记录实际运行过的检查，不能把模拟检查说成上游实测。
